# ===================================================================
# SMART PLACEMENT PORTAL (CLIENT) — ENVIRONMENT CONFIGURATION
# ===================================================================

# Production Render Backend API URL (Optional in local dev due to Vite proxy)
# When deployed to Vercel (https://smartplacementportal.vercel.app), set this to your Render service URL e.g.:
# VITE_API_URL=https://smartplacementportal.onrender.com/api
VITE_API_URL=
